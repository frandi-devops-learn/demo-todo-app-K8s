# -----------------------------
# Wait for MicroK8s on all nodes
# -----------------------------
- name: Wait for MicroK8s ready on all nodes
  hosts: all
  become: yes
  gather_facts: no

  tasks:
    - name: Wait for MicroK8s (SSM safe)
      shell: microk8s status | grep -q "running"
      register: result
      retries: 15
      delay: 10
      until: result.rc == 0


# -----------------------------
# Generate Join Command
# -----------------------------
- name: Generate join command on master
  hosts: masters
  become: yes
  gather_facts: no

  tasks:
    - name: Ensure MicroK8s is ready
      command: microk8s status --wait-ready

    - name: Generate join command
      command: microk8s add-node
      register: join_output

    - name: Extract join command
      set_fact:
        join_command: "{{ join_output.stdout | regex_search('microk8s join .*') }}"


# -----------------------------
# Join Workers
# -----------------------------
- name: Join workers to cluster
  hosts: workers
  become: yes
  gather_facts: no

  tasks:
    - name: Ensure MicroK8s is ready
      command: microk8s status --wait-ready

    - name: Join cluster (idempotent)
      command: "{{ hostvars[groups['masters'][0]].join_command }}"
      register: join_result
      failed_when: false
      changed_when: "'already' not in join_result.stdout"
      when: hostvars[groups['masters'][0]].join_command is defined


# -----------------------------
# Configure Cluster
# -----------------------------
- name: Configure cluster (labels + metrics)
  hosts: masters
  become: yes
  gather_facts: no

  tasks:
    - name: Wait until all nodes are Ready
      command: microk8s kubectl get nodes --no-headers
      register: nodes_output
      retries: 15
      delay: 20
      until: nodes_output.stdout_lines | length >= 3

    # -----------------------------
    # Get REAL node names (important)
    # -----------------------------
    - name: Get all node names
      command: microk8s kubectl get nodes -o jsonpath='{.items[*].metadata.name}'
      register: node_names

    - name: Convert node names to list
      set_fact:
        all_nodes: "{{ node_names.stdout.split() }}"

    - name: Set master node name
      set_fact:
        master_node: "{{ all_nodes[0] }}"

    - name: Set worker nodes
      set_fact:
        workers_list: "{{ all_nodes[1:] }}"

    # -----------------------------
    # Label Nodes
    # -----------------------------
    - name: Label master node
      command: >
        microk8s kubectl label node {{ master_node }}
        node-role=master --overwrite

    - name: Label worker-01
      command: >
        microk8s kubectl label node {{ workers_list[0] }}
        node-role=worker-01 --overwrite
      when: workers_list | length > 0

    - name: Label worker-02
      command: >
        microk8s kubectl label node {{ workers_list[1] }}
        node-role=worker-02 --overwrite
      when: workers_list | length > 1

    # -----------------------------
    # Enable Metrics Server
    # -----------------------------
    - name: Enable metrics server
      command: microk8s enable metrics-server
      register: metrics_result
      failed_when: false

    # -----------------------------
    # Taint Master Node
    # -----------------------------
    - name: Taint master node
      command: >
        microk8s kubectl taint nodes {{ master_node }}
        node-role=master:NoSchedule --overwrite